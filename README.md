# The Topology Improvement Proposals repository

## Installation

Clone this repo:

    $ git clone https://github.com/topology-foundation/TIPs.git

If you haven't already, install bundler:

    $ gem install bundler

And then execute:

    $ bundle install

Serve the site:

    $ bundle exec jekyll serve
